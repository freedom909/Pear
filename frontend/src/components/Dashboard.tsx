import LogoutButton from "./LogoutButton";

const Dashboard = () => {
  return (
    <div>
      <h1>Welcome to your dashboard</h1>
      {/* Add charts, widgets, user info here */}
  
     <LogoutButton/>
    </div>
    
  );
};

export default Dashboard;
